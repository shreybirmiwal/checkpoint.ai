import React from 'react'
import { useParams } from "react-router-dom";

function AnalyticsStudents() {
    let { id } = useParams();

    return (
        <div>

        </div>
    )
}

export default AnalyticsStudents
